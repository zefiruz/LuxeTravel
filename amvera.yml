meta:
  environment: docker
  toolchain:
    name: docker

build:
  dockerfile: Dockerfile

run:
  containerPort: "80"
  # persistenceMount: /data   # раскомментируй, если понадобятся постоянные файлы